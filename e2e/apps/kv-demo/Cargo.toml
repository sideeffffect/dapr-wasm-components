[package]
name = "kv-demo"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Example Dapr app component: exercises state, pub/sub, secrets and locks through dapr-wasm-components:interfaces"
publish = false

[dependencies]
wit-bindgen = "0.58"
